import * as MATCHACTIONS from '../constants/action-types/match';

const initialState = {};

function rc_match(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default rc_match;
